import {
    expect,
    describe,
    it
} from "vitest";

import * as Core from "@/core"
import * as Algorithm from "@/algorithm"


describe("MultilayerNetwork", () => {

    describe("L", () => {

        /*it("ok", () => {
            const network = new Core.MultilayerNetwork();
            expect(Algorithm.MultilayerNetwork.L({ network })).toBe(0);
        });

        it("ok - single", () => {
            const network = new Core.SingleLayerNetwork();
            expect(Algorithm.MultilayerNetwork.L({ network })).toBe(1);
        });*/
    });
});